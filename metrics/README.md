# Comparable measurements

`experiments.csv` starts empty. Record only actual attempts: revision, fixture,
method, elapsed time, manual steps, outcomes, failures and evidence path. Use the
same scenario/environment for before/after comparisons, disclose warm caches,
retries and participant familiarity. Do not translate setup duration into a
claim about Bob productivity. Bobcoin values must come from real Bob summaries.
