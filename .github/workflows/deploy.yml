# =============================================================================
# LMS Maritime — Build & Deploy Pipeline
#
# Flow: push to main → build images in parallel → push to GHCR → SSH deploy
# VM only pulls pre-built images — no build on production server.
#
# Required GitHub Environment "production" with:
#   Secrets: DEPLOY_SSH_PRIVATE_KEY, DEPLOY_KNOWN_HOSTS
#   Variables: DEPLOY_HOST, DEPLOY_USER, DEPLOY_APP_DIR
# =============================================================================

name: Build & Deploy

on:
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".claude/**"
  workflow_dispatch:
    inputs:
      skip_deploy:
        description: "Build images only (skip deploy to VM)"
        type: boolean
        default: false

permissions:
  contents: read
  packages: write

concurrency:
  group: deploy-production
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: ghcr.io/${{ github.repository_owner }}/lms-backend
  FRONTEND_IMAGE: ghcr.io/${{ github.repository_owner }}/lms-frontend

jobs:
  # ── Build Backend Image ──────────────────────────────────────────────
  build-backend:
    if: github.repository == 'linhlinhlin/LMS_hohulili'
    name: Build Backend
    runs-on: ubuntu-latest
    timeout-minutes: 25
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push backend
        id: build
        uses: docker/build-push-action@v7
        with:
          context: ./backend
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:${{ github.sha }}
          cache-from: type=gha,scope=backend
          cache-to: type=gha,scope=backend,mode=max
          provenance: false

  # ── Build Frontend Image ─────────────────────────────────────────────
  build-frontend:
    if: github.repository == 'linhlinhlin/LMS_hohulili'
    name: Build Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 25
    outputs:
      digest: ${{ steps.build.outputs.digest }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push frontend
        id: build
        uses: docker/build-push-action@v7
        with:
          context: ./fe
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}:latest
            ${{ env.FRONTEND_IMAGE }}:${{ github.sha }}
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,scope=frontend,mode=max
          provenance: false

  # ── Deploy to Production VM ──────────────────────────────────────────
  deploy:
    name: Deploy Production
    needs: [build-backend, build-frontend]
    # Gate: only deploy when VM is provisioned & reachable.
    # Set repo variable DEPLOY_ENABLED=true to resume auto-deploy
    # after starting the GCP VM (gcloud compute instances start lms-production).
    if: github.repository == 'linhlinhlin/LMS_hohulili' && vars.DEPLOY_ENABLED == 'true' && github.event.inputs.skip_deploy != 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: production
      url: https://holilihu.online

    steps:
      - name: Configure SSH
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          printf '%s\n' "${{ secrets.DEPLOY_KNOWN_HOSTS }}" > ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: Deploy via SSH
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_USER: ${{ vars.DEPLOY_USER }}
          DEPLOY_APP_DIR: ${{ vars.DEPLOY_APP_DIR }}
          DEPLOY_SHA: ${{ github.sha }}
        run: |
          ssh -i ~/.ssh/id_ed25519 -o BatchMode=yes -o StrictHostKeyChecking=yes \
            "$DEPLOY_USER@$DEPLOY_HOST" \
            "DEPLOY_APP_DIR='$DEPLOY_APP_DIR' DEPLOY_SHA='$DEPLOY_SHA' bash -se" <<'DEPLOY_EOF'
          set -euo pipefail
          cd "$DEPLOY_APP_DIR"

          echo "=== LMS Maritime — CI/CD Deploy ==="
          echo "Revision: $DEPLOY_SHA"

          # Pin the VM to the pushed revision, then delegate the rollout to the
          # same script used for verified manual production deploys.
          git fetch --prune origin
          git checkout --force "$DEPLOY_SHA"
          bash ./deploy.sh
          DEPLOY_EOF

      - name: Post-deploy summary
        run: |
          {
            echo "### Deploy completed"
            echo ""
            echo "- Revision: \`${{ github.sha }}\`"
            echo "- Backend: \`${{ env.BACKEND_IMAGE }}:${{ github.sha }}\`"
            echo "- Frontend: \`${{ env.FRONTEND_IMAGE }}:${{ github.sha }}\`"
            echo "- URL: https://holilihu.online"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Remove SSH material
        if: always()
        run: rm -f ~/.ssh/id_ed25519 ~/.ssh/known_hosts
